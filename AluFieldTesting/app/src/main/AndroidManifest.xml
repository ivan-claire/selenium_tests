<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.testing.alcatel.alufieldtesting" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
      <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:configChanges="keyboardHidden|orientation|screenLayout"
        android:theme="@style/Theme.TranslucentActionBar" >

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB2wIX-mLfLvr_nY7LwNnC35MTU4Zf-mlk" />

        <activity
            android:name=".Forcing"
            android:label="@string/title_activity_forcing"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".Testing"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_testing"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".StationaryTests"
            android:label="@string/title_activity_stationary_tests"
            android:theme="@style/Theme.TranslucentActionBars" >
        </activity>
        <activity
            android:name=".MobilityTests"
            android:label="@string/title_activity_mobility_tests" >
        </activity>
        <activity
            android:name=".Propertiess"
            android:label="@string/title_activity_propertiess"
            android:theme="@style/Theme.TranslucentActionBars" >
        </activity>
        <activity
            android:name=".ViewProgress"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_view_progress"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".CellInformation"
            android:label="@string/title_activity_cell_info"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme" >
        </activity>

        <activity
            android:name=".RFmeasurements"
            android:label="@string/title_activity_rfmeasurement"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppThemes.NoActionBar">

        </activity>
        <activity
            android:name=".AndroidDatabaseManager">

        </activity>

        <activity
            android:name=".CsVoiceCalls"
            android:label="CS Voice Call"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme" >
        </activity>

        <activity
            android:name=".PsDataCalls"
            android:label="PS Data Call"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme" >
        </activity>
    </application>

</manifest>
